#include <stdio.h>

int main();
{
	int numeros[10];
	int maior=0, i;
	
	for(i=0;i<10;i++)
	{
		printf("\n Entre com o numero:");
		scanf("%d", &numeros);
		if(numeros)[i]>maior)
		{
			maior = numeros[i];
		}
	//exbir na tela o maior valor lido
	printf("\n O maior valor=%d", maior);
	
	return 0;
	}
}
